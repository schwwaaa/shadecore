[workspace]
members = [
  "crates/shadecore-cli",
  "crates/shadecore-engine",
  "crates/shadecore-scratchpad",
]
resolver = "2"

# Workspace default so `cargo run` (from repo root) runs the main ShadeCore binary.
default-members = ["crates/shadecore-cli"]
